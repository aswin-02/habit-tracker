export const metadata = {
  title: 'Login - Habit Tracker',
  description: 'Login to your Habit Tracker account',
}

export default function LoginLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <>{children}</>
}
